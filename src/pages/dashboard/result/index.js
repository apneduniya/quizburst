import Head from 'next/head';
import { Inter } from 'next/font/google';
import Navbar from '@/pages/Navbar';
import { Box } from '@/pages/Card';
import { useState } from 'react';
import { useRouter } from 'next/router';

import { useUser } from '@auth0/nextjs-auth0/client';
import Script from 'next/script';
import { Button } from '@mantine/core';

const Router = () => {
  const router = useRouter();

  return router;
}

export default function Quiz() {

  const router = useRouter();
  const data = router.query;
  console.log(data);

  const [overlay, setOverlay] = useState(false);

  const { user, error, isLoading } = useUser();

  if (isLoading) return <div>Loading...</div>;
  if (error) return <div>{error.message}</div>

  if (user) {

    const router = Router();

    const handleSubmit = async (e) => {
      e.preventDefault();

      router.push({
        pathname: '/dashboard',
      });
    }

    return (
      <>
        <Head>
          <title>QuizBurst | Result</title>
          <meta name="description" content="Generated by create next app" />
          <meta name="viewport" content="width=device-width, initial-scale=1" />
          <link rel="icon" href="/favicon.ico" />
        </Head>
        <Script src="https://unpkg.com/@lottiefiles/lottie-player@latest/dist/lottie-player.js" />
        <Navbar buttonText={"Sign Out 🙃"} buttonLink={"/api/auth/logout"} />
        <div
          className="dashboard-container"
          id="dashboard"
          style={{ margin: "160px 0", padding: "0 20px", position: "relative", height: "auto", width: "100%", display: "flex", flexDirection: "column", justifyContent: "space-evenly", alignItems: "center" }}
        >
            <lottie-player src="https://assets6.lottiefiles.com/packages/lf20_aao5ezov.json" background="transparent" speed="1" style={{ height: 290, width: 290 }} loop autoplay></lottie-player>
            <Box text={"Congratulations! You got 8 correct."} />
            <Button type="button" style={{ marginTop: "20px" }} onClick={handleSubmit} >Play Again</Button>
        </div>
      </>
    );
  }

  // eslint-disable-next-line @next/next/no-html-link-for-pages
  return <a href="/api/auth/login">Login</a>;
}

